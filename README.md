# Projeto para oficina da UNIFG

## Necessário node instalado na máquina

## Para instalar as dependências use o comando `npm install`

## Use o comando `npm run dev` para rodar o projeto localmente. Verifique a rota utiliza no terminal.
